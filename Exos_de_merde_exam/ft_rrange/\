#include <stdlib.h>
#include <stdio.h>

int     ft_rrange(int start, int end)
{
	int i = 0;
	int	t_start = start;
	int t_end = end;
	while (t_start != t_end)
	{
		t_start++;
		i++;
	}
	printf("%d\n", i);
	return (i);
}

int	main()
{
	ft_rrange(-1, 2);
	return (0);
}
